name: Black Formatter

on: [ push, pull_request ]

jobs:
  black:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install black

      - name: Check Black formatting
        run: black --check --line-length 80 .

      - name: Annotate Black format check failures
        if: ${{ failure() }}
        run: |
          echo "::group::Files that require formatting:"
          black --diff --line-length 80 . 2>&1 | grep -Po "would reformat \K.*"
          echo "::endgroup::"
        shell: bash
